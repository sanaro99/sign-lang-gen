# Decision Log

One line per non-obvious choice, with the reason. This becomes the methods section
of the final report — future-us will not remember why we did any of this.

| Date | Decision | Why | Who |
|---|---|---|---|
| 2026-07-11 | Re-implement CHI 2025 Module 1 from the paper rather than forking code | No official code exists (verified: Apple ML page, arXiv, ACM DL, `apple/ml-*`, author GitHubs). Work was done entirely at Apple with internal assets. | team |
| 2026-07-11 | Start on ASLG-PC12; request ASLLRP access in parallel | ASLG-PC12 is instantly available and lets us build the pipeline this week. ASLLRP is what the paper actually used and is needed for a faithful baseline. | team |
| 2026-07-11 | `static_shots: 8` in the baseline config, not ~1,474 | Their full pool needed "multi-prompting" batching. Running that faithfully is expensive. **Open question — resolve before Week 6:** does our baseline use 8 shots (cheap, but not the paper's baseline) or reproduce multi-prompting (faithful, costly)? Recommend: run both, report both. An 8-shot static baseline is arguably a *strawman* that flatters our RAG condition. | team |
| 2026-07-11 | Pin `gpt-4o-2024-05-13`; keep an open-model path | The paper's snapshot may be deprecated mid-quarter. If it disappears, our baseline is unreproducible. | team |
| 2026-07-12 | Work order: prerequisites → RAG → context → (optional) reordering | Gloss conventions/eval must be trustworthy before any number; sequence keeps each result defensible. | team |
| 2026-07-12 | Position the **context buffer** as the primary novelty; **reframe RAG** honestly | Landscape review: no prior work applies discourse/paragraph context to text→gloss *production*, but RAG-for-gloss exists (Bangla, arXiv 2511.08507) + MT example selection. RAG delta = first-for-ASL + NMM-integrated + beats Zhang's random multi-prompting. | team |
| 2026-07-12 | Add an **optional reordering pass** (Phase 3 / M3.5) | Gloss ordering (English word order in gloss) is the best-documented LLM failure (Guo/Li/Cohn, NeurIPS 2025). | team |
| 2026-07-12 | **Metric hygiene:** fixed-tokenization SacreBLEU + a semantic metric + a contrastive discourse set; don't lead with corpus BLEU | Corpus BLEU barely moves for the context contribution and undersells it; synthetic-gloss inflation on ASLG-PC12. Prefer ASLLRP for credible claims. | team |
| 2026-07-12 | **De-duplicated split** to guard RAG against leakage | Retrieving near-duplicates of test items inflates BLEU without real generalization. | team |
| 2026-07-12 | Align gloss conventions to ASLG-PC12 **before** any BLEU | Confirmed on real data: corpus uses `X-I`/`DESC-`/kept-punctuation vs. our prompt's `IX`/dropped punctuation; mismatch penalizes the baseline on notation alone. | team |
| 2026-07-12 | Soften the ISL claim | Safe: "no *large, public, Deaf-produced* ISL gloss corpus exists." Small expert-built sets do (e.g. IJSAT 2025). Update `isl_extension.md` + `ATTRIBUTION.md`. | team |
| 2026-07-12 | BLEU uses `sacrebleu` with `tokenize="none"` | Gloss is already whitespace-tokenized; exact matching keeps `X-I`/`DESC-…`/`fs-…` and punctuation whole. Default `13a` would re-split on hyphens and change scores on notation alone. | team |
| 2026-07-12 | De-duplicated, leakage-filtered split (`download_data.py`) | Exact-dedup + drop pool items ≥0.9 cosine to any test item. Confirmed live: retrieval had returned near-identical pool sentences, inflating RAG BLEU. Test kept contiguous to preserve weak discourse adjacency. | team |
| 2026-07-12 | 4-condition comparison uses a **matched 8-example budget** (static vs retrieved) | Isolates the effect of *retrieval* and *context* cleanly. Paper-faithful ~1,474-shot multi-prompting is a separate, later comparison — not needed for the internal ablation. | team |
| 2026-07-12 | First comparison runs on **local `gemma3:4b`** (Ollama), not gpt-4o | Free, and internally consistent (same model across all four conditions). gpt-4o numbers await a key/budget; local numbers are labeled preliminary. | team |
| 2026-07-16 | **Phase A0 done: mined the paper's arXiv TeX source** (`data/paper_src/`, gitignored) | Primary source beats secondary summaries: exact prompts recovered (both tasks + all 4 ablation variants + grammar-rules text), preprocessing Steps 1–4, conventions table, targets verified (0.276; 0.91/0.97). Summary: `docs/primary_source_findings.md`. | team |
| 2026-07-16 | Treat figure alt-text conditional recall "0.05" as a typo for ≈0.95 | Only ≈0.95 is consistent with the paper's own claimed 0.97 average recall and the caption's "high performance across all categories"; state the resolution in the report. | team |
| 2026-07-16 | **Reframe contribution 1 (RAG) as reproduction + extension** — the paper's own appendix already implements anonymized-embedding top-N retrieval (N=50 ≈ 0.279 > all-1,474) | Honesty: we can't claim RAG-for-this-task as ours. Our deltas: leakage-guarded dedup split, k-ablation, cost accounting, retrieval×context combination, anonymization ablation. Context buffer stays the primary novelty (paper is explicitly context-free). | team |
| 2026-07-16 | Faithful baseline = data-prep + 1,474 examples + **limited vocab ON + grammar rules OFF** | That is the paper's published best row (0.276); their grammar rules mostly *lowered* BLEU. Grammar-rules condition becomes an optional ablation, not the baseline. | team |
| 2026-07-16 | Verbatim paper text (prompts, appendix) stays **gitignored** in `data/paper_src/`; tracked docs summarize in our own words | Repo convention: "paper text/figures are not reproduced." Whether short prompt transcriptions may be committed to `prompts/` is an open team decision (below). | team |
| 2026-07-16 | NMM gold labels: self-annotate English test sentences with a written rubric + Cohen's κ, mirroring the paper's 4-researcher correction | A0 confirmed the paper's labels are English-text properties re-annotated by humans (their Step 4) — so this is the faithful replication path and needs no ASLLRP. Kit: `docs/nmm_annotation_rubric.md`, `scripts/nmm_annotation.py`. | team |
| 2026-07-16 | Add **NCSLGR** (Boston University / Neidle; the ASLLRP family) as a **real-ASL testbed** while the Sign Bank access is pending | NCSLGR is publicly mirrored (DePaul ELAN export, no video), gives ~1,875 **real, Deaf-produced** ASL gloss pairs with true ASLLRP conventions, and loads with stdlib — the honest gap ASLG-PC12 (synthetic) leaves. It is *not* an independent corpus (same project family as the gated data) and its licence terms are unstated → treat exactly like ASLLRP: local research/education use, gitignored, never committed. `load_ncslgr()` in `data/loaders.py`; see `docs/datasets.md`. | team |
| 2026-07-16 | NCSLGR split is **document-level** (hold out whole stories), not a tail slice | Prevents a story's sentences from straddling pool/test (stronger leakage guard than ASLG-PC12's tail block) while preserving within-document adjacency for the context conditions. `scripts/download_data.py --dataset ncslgr` → `data/processed/ncslgr/`. | team |
| 2026-07-16 | Do **not** score NCSLGR with `baseline_gloss.md`; author a separate real-ASL prompt (`prompts/asllrp_gloss.md`) | NCSLGR gloss uses real conventions (`fs-`, `IX-`, `POSS-`, `#loan`, `DCL:`/`BCL:`, `5"…"`) and ASL grammar; the ASLG-PC12 prompt would score near-zero on **notation** alone (the same trap we fixed for ASLG-PC12). Any BLEU from the provisional real-ASL prompt is preliminary. This also gives us a head start on Phase B1 before ASLLRP lands. | team |
| 2026-07-16 | NCSLGR's **signing-based** NMM tiers are a *comparison*, not gold | The EAF files carry `yes-no question`/`wh question`/`conditional/when`/`negative` tiers, but they mark the **signing** — exactly what the paper's Step 4 corrected *away from* toward the English text. Confirmed live: their `yes_no_question` fires on "asked … **if** he was alright" (embedded → our text rubric = 0). Using them as gold would contradict `docs/nmm_annotation_rubric.md`. | team |
| 2026-07-16 | **Defer PHOENIX-2014T** to a documented option, not built now | Real DGS gloss and scoreable, but German/DGS makes our English→ASL prompt and the four NMM labels inapplicable — a pure "does the plumbing run" cross-lingual test that duplicates the exploratory ISL story with weaker fit. Noted in `docs/datasets.md`. | team |
| 2026-07-16 | **Fix `.gitignore`: anchor `data/` → `/data/`** | The bare `data/` rule also matched `src/aslgloss/data/`, so the entire data-loader module (`loaders.py`, `paragraphs.py`, `__init__.py`) was **never tracked** — a fresh clone's package would fail to import. Anchoring to the repo root keeps the corpus ignored while restoring the module to version control. | team |
| 2026-07-16 | **DAI access granted → implemented `load_asllrp()`** against the real DAI 2 export | Parses `xml_extract_*.xml`: `<UTTERANCE>` → `<TRANSLATION>` (English) + ordered `<SIGN><LABEL>` gloss; optional signing-NMM from sentence-type `<NON_MANUAL>` tiers. First 4 collections (Ben narratives) → 156 pairs. Data lives in gitignored `data/dai/`, never committed. `tests/test_asllrp.py`. | team |
| 2026-07-16 | ASLLRP gloss order = **document order**, not frame-sorted | Doc order matched chronological (frame) order in 154/156 utterances, and 122 signs carry no dominant-hand frame — frame-sorting would mix a real-frame/position fallback (the scale-mix bug class). Document order is faithful ("chronologically spliced" in the export) and robust. | team |
| 2026-07-16 | ASLLRP `conditional` label counts only `conditional/when` **value == "conditional"** | The DAI tier conflates true conditionals with temporal "when"; the paper's text rubric separates them, so temporal "when" is excluded (mirrors `docs/nmm_annotation_rubric.md`). Still signing-based → a starting point, not final gold (paper Step 4). | team |
| 2026-07-16 | **`build_paragraphs` now honors real `doc_id` boundaries** (fixed-size chunking only when `doc_id` is absent) | The context contribution *cannot* be honestly evaluated on auto-chunked pseudo-paragraphs — the whole reason we wanted real discourse. ASLLRP collections are real narratives; re-chunking into arbitrary 5-sentence windows (and pulling context across collection boundaries) would reproduce the exact evaluation we called inadequate. ASLG-PC12 (`doc_id=None`) behavior is unchanged. | team |
| 2026-07-16 | First **ASLLRP four-condition run**: real-ASL prompt, `constrain_vocab: off`, `gemma4:latest`, n=20 | Get preliminary real-ASL results now. Explicitly NOT the paper's faithful baseline: no Sign Bank dictionary yet, corpus subset (29/84 files), provisional prompt, local CPU model, and n=20 ≈ one narrative. BLEU self-labeled `dataset=asllrp` in `summary.csv`; NMM P/R deferred (no gold). Configs `configs/asllrp_*.yaml`. | team |
| 2026-07-17 | **Full 220-sentence ASLLRP four-condition run** supersedes the n=50 pilot; all four conditions tie at BLEU-4 ≈ 0.041–0.043 | Neither RAG nor context moved BLEU on the full real-ASL test set; the earlier n=50 "RAG dominates" was a small-sample (question-heavy slice) artifact. n-gram precision 37→11→3→1 + BP≈0.73 = "right vocabulary, wrong exact sequence"; 20% of reference tokens are non-lexical and not text-derivable. Verdict: reproduction ❌, infrastructure ✅, contributions ⚠️ unsupported on BLEU. Full write-up `docs/results_report_asllrp.md`. | team |
| 2026-07-17 | **HOLD on vocabulary constraint** until the official Sign Bank arrives; built only a corpus-derived approximation as a documented finding | The `constrain_vocab` flag only *flags* OOV, it does not constrain generation — really using a dictionary needs prompt-injection (a soft hint) or output post-filtering, neither of which is the paper's output-vocab constraint. Coverage analysis (59% input-word reach, 20% non-lexical ceiling) plus the 37% unigram precision predicts a glossary would nudge BLEU-1 but not BLEU-4, so an exploratory condition isn't worth long local runs now. Do it once, properly, with the official Sign Bank. `scripts/build_gloss_dictionary.py`, `data/processed/asllrp/gloss_dictionary.json`. | team |
| 2026-07-19 | **Measurement stack fixed before any further scored run** (gap G2): secondary "reachable-token" score (`normalize_real_asl` — strips loci/handedness/classifiers/prosody from BOTH sides), chrF, exact-match, ROUGE-L (whole-token), per-n-gram precisions + BP columns, and **paired-bootstrap CI + p-value vs. same-dataset baseline** in `evaluate.py`. Dead `summarize_run` removed. | Raw BLEU-4 vs. raw ASLLRP notation is pinned at the ~0.04 floor, which is how the four-condition run produced an uninterpretable null; and the n=50 "RAG dominates" artifact happened because deltas shipped without intervals. Normalized scores are reported ALONGSIDE raw, never against the paper's 0.276. | team (pending review) |
| 2026-07-19 | **Step-2 faithful cleaning implemented** (`src/aslgloss/data/asllrp_clean.py`, `download_data.py --faithful-clean`), with two documented interpretations: locus "unification" = per-sentence canonical relabeling (i, j, k… preserving coreference); trailing `+` stripped (repetition) but internal `+` kept (compounds per Appendix D) | Recipe recovered in Phase A0 was documented but never implemented (gap G7). PROVISIONAL until checked against the verbatim `data/paper_src/EXTRACTION_NOTES.md` on the machine that holds it. | team (pending review) |
| 2026-07-19 | **Anonymization: reproduce AND ablate** (resolves the open question below): `retrieval.anonymize` knob + paired configs `ncslgr_rag` / `ncslgr_rag_anon` with distinct index files. Implementation is a documented capitalization heuristic (names→"they"/"their"), not NER | The paper's appendix says anonymization is *why* their RAG worked (names hijack embedding similarity); a knob lets us measure that claim under our leakage-guarded split. The paper doesn't state their method; heuristic limitations are documented in `retrieval/anonymize.py`. | team (pending review) |
| 2026-07-19 | **Multi-prompting structure implemented** (gap G4): `shots_as_messages` + `shot_batch_size` config → examples as repeated ASSISTANT messages (`LLMClient.complete_messages`); `resolve_prompt()` fallback lets configs reference gitignored faithful prompt transcriptions without committing paper text | Gives open question #1 (baseline fairness) its missing mechanism: the ~1,474-shot paper-style baseline is now *runnable* (`configs/ncslgr_baseline_multiprompt.yaml`), so we can report BOTH comparators instead of arguing about them. Also keeps open question #6 (commit transcriptions?) a pure policy call — the plumbing no longer forces it. | team (pending review) |
| 2026-07-19 | **Discourse probe set v0 drafted** (gap G1/M3): 54 constructed passages, 5 categories (pronoun_referent 18, negation_scope 12, topic_continuity 10, ellipsis 8, nmm_scope 6), tracked in `probes/` (original English → no license issue), loader + validation + `build_probe_split.py` (runnable split + blank blind-coding sheet), paired configs `probes_baseline` / `probes_context` | The context buffer is the project's main novelty and had NO instrument that could detect it. Probes are human-coded (coder_question/correct_answer), never BLEU-scored. **DRAFT: requires two-member review before any graded use** — protocol in `probes/README.md`. | team (pending review) |
| 2026-07-19 | **Second dev machine bootstrapped** (macOS): venv + deps, ASLG-PC12 + NCSLGR splits + FAISS indexes rebuilt locally, Ollama + `gemma3:4b` installed; `ncslgr_rag` and `ncslgr_rag_anon` smoke-run (n=5, `--no-nmm`) end-to-end through the new `evaluate.py` | Confirms a fresh clone reaches a working pipeline from the repo alone (the point of the `.gitignore` fix), and exercises retrieval + anonymization + new metrics live before anyone spends money on gpt-4o. Smoke numbers are meaningless (n=5) and stay out of tracked results. | team |

## Open questions

- [ ] **Baseline fairness.** See above. This is the single biggest threat to our claims: if we compare RAG-8-shot against a static-8-shot baseline, we test retrieval; if we compare against the paper's ~1,474-shot multi-prompting, we test retrieval *and* efficiency. Decide explicitly and defend it.
- [ ] **Test split.** ASLG-PC12 ships a single split. `download_data.py` currently takes a naive tail slice. Define a proper, documented split before any number goes in the report.
- [ ] **Discourse probe set — v0 DRAFTED (2026-07-19), review pending.** 54 passages in `probes/discourse_probes_v0.jsonl` with a coding protocol (`probes/README.md`). Remaining human step: two team members review every probe (truly ambiguous without context? truly resolvable with it?), edit/drop, log the review here, bump to v1. No graded number rests on v0.
- [ ] **Gloss conventions.** Ours must be written down (docs/gloss_conventions.md) and match ASLG-PC12's tokenization or the BLEU comparison is meaningless.
- [ ] ~~Should we email Colin Lea / Han Zhang to ask whether the Module 1 prompts can be shared?~~ **Mostly moot (2026-07-16):** the prompts turned out to be in the paper's own figure/appendix TeX (Phase A0). An email would only clarify the residual figure-vs-alt-text punctuation discrepancy and the unpublished split seed.
- [ ] **May faithful transcriptions of the paper's three short prompt templates be committed to `prompts/` (with citation)?** They are short, functional instructions needed for the reproduction, but the repo convention says no paper text in tracked files. Until decided, verbatim wording lives only in gitignored `data/paper_src/EXTRACTION_NOTES.md`.
- [x] ~~How exactly to reproduce the paper's RAG appendix variant (anonymization names→pronouns) alongside our retriever — reproduce, ablate, or both?~~ **Decided 2026-07-19: both** — `retrieval.anonymize` knob, paired `ncslgr_rag` / `ncslgr_rag_anon` configs (see log entry). Open sub-question: upgrade the heuristic to real NER if the ablation shows an effect.
